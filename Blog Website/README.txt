A blog website where the user can see all the previous posts which are stored in a MongoDB database. By adding /compose to 
the end of the URL, the user can add another post which will be added to the database. This was built using HTML, CSS, JS, 
Express.JS, EJS, and MongoDB.
